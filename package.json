{
  "name": "exemplar",
  "module": "src/index.ts",
  "type": "module",
  "dependencies": {
    "@kitajs/html": "^3.0.0",
    "@kitajs/ts-html-plugin": "^1.1.1",
    "kysely": "^0.26.3",
    "pg": "^8.11.3",
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "@types/pg": "^8.10.3",
    "bun-types": "latest",
    "daisyui": "^3.8.2",
    "kysely-codegen": "^0.10.1",
    "prettier": "3.0.3",
    "tailwindcss": "^3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "db:generate:migration": "bun scripts/db/generate-migration.ts --name",
    "db:generate:types": "bun scripts/db/generate-types.ts",
    "db:migrate:down:dev": "bun scripts/db/migrate-down.ts",
    "db:migrate:down:test": "DATABASE_NAME=test bun scripts/db/migrate-down.ts",
    "db:migrate:up:dev": "bun scripts/db/migrate-up.ts",
    "db:migrate:up:test": "DATABASE_NAME=test bun scripts/db/migrate-up.ts",
    "lint:check": "prettier",
    "lint:write": "prettier --write",
    "start:prod": "bun run src/index.ts",
    "start:watch": "bun --hot src/index.ts",
    "tailwind:build": "bun run tailwindcss -i ./src/tailwind.css -o ./assets/tailwind.css",
    "tailwind:watch": "bun run tailwindcss -i ./src/tailwind.css -o ./assets/tailwind.css --watch",
    "web-components:build": "WATCH_WEB_COMPONENTS=false bun run scripts/build-web-components.ts",
    "web-components:watch": "WATCH_WEB_COMPONENTS=true bun run scripts/build-web-components.ts"
  }
}
